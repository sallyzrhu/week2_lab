-- Get the first and last name of every player in the database.

-- sqlite3 baseball.sqlite3
-- .schema
-- .mode columns
-- .headers on
-- .read ____
-- .quit

-- SELECT __ FROM ____
-- INNER JOIN ___ ON __
-- WHERE 
-- AND
-- GROUP BY___
-- ORDER BY___
-- LIMIT ___;

-- COUNT(*), AVG(column), SUM(column), MIN(column), MAX(column)
